# Angular SPA routing: serve files if they exist, otherwise index.html
location / {
    try_files $uri $uri/ /index.html;
}

# Long-cache immutable hashed assets
location ~* \.(?:js|css|woff2)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
}
